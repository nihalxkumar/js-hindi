"use strict"; // treat all JS code as newer version(newer classes, modules). ek baar likh diya abb neeche saara code strict mein read hoga

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high priority

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint    for big numebrs. trading makets, reddit, facebook etc
// string => ""
// boolean => true/false
// null => standalone value CAN BE ASSIGNED let state = ""
// undefined => 
// symbol => unique Zyadatar implementation react.js mein. same like figma webflow

//masti with object. interview qn

// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object

